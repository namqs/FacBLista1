/*O custo ao consumidor de um carro novo é a soma do custo de fábrica com a porcentagem do distribuidor e dos impostos (aplicados ao custo
 de fábrica). Desenvolver um algoritmo que calcule o custo ao consumidor de determinado carro.*/

 /*Algoritmo:
  1º: solicitar o custo de fábrica e a porcentagem do distribuidor e dos impostos
  2º cc = cf + (pd*cf)/100 + (pi*cf)/100
  3º: exibir resultado*/
#include <stdio.h>
void main ()
{
 

 
 

}
